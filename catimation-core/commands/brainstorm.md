---
description: 创意头脑风暴 — 用 catimation-brainstorm 在开放/高价值创意上先定方向
---

载入 **catimation-brainstorm** 技能(用 Skill 工具加载 `catimation-brainstorm`),用 `ask_user` 卡片一次一项地和用户共创方向(气质/景别/运镜/时长/风格等),收敛出明确诉求后,再交给 `/director` 写镜头或 `/gen-image`、`/gen-video` 出片。

诉求在下方:

$ARGUMENTS
